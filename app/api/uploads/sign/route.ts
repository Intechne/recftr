import {NextRequest,NextResponse} from "next/server";
import {contentSession,portalSession} from "@/lib/auth";
import {createUpload,PRIVATE_BUCKET,PUBLIC_BUCKET,publicUrl,safeFileName} from "@/lib/storage";
export const dynamic="force-dynamic";
const IMAGE=['image/jpeg','image/png','image/webp','image/gif','image/svg+xml'];
const VIDEO=['video/mp4','video/webm','video/quicktime'];
const DOC=['application/pdf','image/jpeg','image/png','image/webp','application/msword','application/vnd.openxmlformats-officedocument.wordprocessingml.document','application/vnd.ms-excel','application/vnd.openxmlformats-officedocument.spreadsheetml.sheet'];
export async function POST(req:NextRequest){try{const b=await req.json();const purpose=String(b.purpose||'');const mime=String(b.mimeType||'').toLowerCase();const size=Number(b.size)||0;const fileName=safeFileName(String(b.fileName||'file'));let bucket=PUBLIC_BUCKET,prefix='media',max=25*1024*1024,allowed=[...IMAGE,...VIDEO];if(purpose==='team-doc'){const s=await portalSession(req);if(!s?.teamNum)return NextResponse.json({error:'Yetkisiz'},{status:401});bucket=PRIVATE_BUCKET;prefix=`teams/${s.teamNum}/documents`;max=20*1024*1024;allowed=DOC;}else if(purpose==='team-logo'){const s=await portalSession(req);if(!s?.teamNum)return NextResponse.json({error:'Yetkisiz'},{status:401});prefix=`teams/${s.teamNum}/logo`;max=5*1024*1024;allowed=IMAGE;}else{if(!(await contentSession(req)))return NextResponse.json({error:'Yetkisiz'},{status:401});if(purpose==='document'){prefix='documents';max=50*1024*1024;allowed=DOC;}else if(purpose!=='media')return NextResponse.json({error:'Geçersiz yükleme amacı'},{status:400});}if(size<=0||size>max)return NextResponse.json({error:`Dosya boyutu geçersiz. En fazla ${Math.round(max/1024/1024)} MB.`},{status:400});if(mime&&!allowed.includes(mime))return NextResponse.json({error:'Bu dosya türüne izin verilmiyor.'},{status:400});const path=`${prefix}/${fileName}`;const signed=await createUpload(bucket,path,false);return NextResponse.json({bucket,path,signedUrl:signed.signedUrl,token:signed.token,url:bucket===PUBLIC_BUCKET?publicUrl(path):''});}catch(e:any){return NextResponse.json({error:e?.message||'Yükleme bağlantısı oluşturulamadı.'},{status:500});}}
