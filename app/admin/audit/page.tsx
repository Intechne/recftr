"use client";
import {useEffect,useState} from "react";
export default function Page(){const [list,setList]=useState<any[]>([]);useEffect(()=>{fetch('/api/audit').then(r=>r.json()).then(setList)},[]);return <div className="max-w-6xl"><h1 className="font-display text-[25px] font-bold">İŞLEM GEÇMİŞİ</h1><p className="text-[13px] text-ink/55">Kritik CMS değişikliklerinin son 100 kaydı.</p><div className="mt-5 overflow-x-auto rounded-xl border border-ink/15 bg-white"><table className="w-full min-w-[850px] text-left text-[12px]"><thead className="bg-ink text-white"><tr>{['ZAMAN','KULLANICI','İŞLEM','VARLIK','ID','DETAY'].map(x=><th key={x} className="px-4 py-3">{x}</th>)}</tr></thead><tbody>{list.map(a=><tr key={a.id} className="border-t border-ink/10"><td className="px-4 py-3">{new Date(a.created_at).toLocaleString('tr-TR')}</td><td className="px-4">{a.actor}</td><td className="px-4 font-bold">{a.action}</td><td className="px-4">{a.entity}</td><td className="px-4">{a.entity_id}</td><td className="px-4"><code>{JSON.stringify(a.details)}</code></td></tr>)}</tbody></table></div></div>}
