"use client";
import {useEffect,useState} from "react";import {uploadFile} from "@/lib/client-upload";
export default function Page(){const [list,setList]=useState<any[]>([]),[file,setFile]=useState<File|null>(null),[title,setTitle]=useState(''),[event,setEvent]=useState(''),[alt,setAlt]=useState(''),[caption,setCaption]=useState(''),[msg,setMsg]=useState(''),[busy,setBusy]=useState(false);const load=()=>fetch('/api/media?all=1').then(r=>r.json()).then(setList);useEffect(()=>{load()},[]);async function save(){if(!file||!title)return setMsg('⚠ Başlık ve dosya zorunlu.');setBusy(true);try{const u=await uploadFile('media',file);const r=await fetch('/api/media',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({title,type:file.type.startsWith('video/')?'VİDEO':'FOTO',event_slug:event,path:u.path,url:u.url,mime_type:file.type,size_bytes:file.size,alt_text:alt,caption,published:true})});if(!r.ok)throw new Error((await r.json()).error||'Kaydedilemedi');setMsg('✓ Medya yüklendi.');setFile(null);setTitle('');setEvent('');setAlt('');setCaption('');load()}catch(e:any){setMsg('⚠ '+e.message)}finally{setBusy(false)}}async function del(id:number){if(!confirm('Medya silinsin mi?'))return;await fetch('/api/media',{method:'DELETE',headers:{'Content-Type':'application/json'},body:JSON.stringify({id})});load()}const i='rounded-md border-[1.5px] border-ink/20 bg-white px-3 py-2.5 text-[13px]';return <div className="max-w-6xl"><h1 className="font-display text-[25px] font-bold">MEDYA KÜTÜPHANESİ</h1><p className="text-[13px] text-ink/55">Gerçek Supabase Storage yükleme; public galeri ve içerik kapaklarında kullanılabilir.</p>{msg&&<p className="mt-4 rounded-lg bg-cyan-deep/10 p-3 text-[13px] font-semibold text-cyan-deep">{msg}</p>}<div className="mt-5 grid gap-3 rounded-xl border-2 border-ink bg-white p-5 md:grid-cols-3"><input className={i} placeholder="Başlık" value={title} onChange={e=>setTitle(e.target.value)}/><input className={i} placeholder="Etkinlik slug (opsiyonel)" value={event} onChange={e=>setEvent(e.target.value)}/><input type="file" accept="image/*,video/*" className={i} onChange={e=>setFile(e.target.files?.[0]||null)}/><input className={i} placeholder="Alt text" value={alt} onChange={e=>setAlt(e.target.value)}/><input className={i} placeholder="Caption" value={caption} onChange={e=>setCaption(e.target.value)}/><button disabled={busy} onClick={save} className="rounded-md bg-ink px-4 py-2.5 font-display text-[12px] font-bold text-white">{busy?'YÜKLENİYOR…':'YÜKLE'}</button></div><div className="mt-5 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{list.map(m=><div key={m.id} className="overflow-hidden rounded-xl border border-ink/15 bg-white">{m.type==='VİDEO'?<video src={m.url} className="aspect-video w-full object-cover" controls/>:<img src={m.url} alt={m.alt_text||m.title} className="aspect-video w-full object-cover"/>}<div className="p-3"><p className="truncate font-semibold">{m.title}</p><p className="text-[11px] text-ink/45">{m.type} {m.event_slug&&'· #'+m.event_slug}</p><div className="mt-2 flex gap-3 text-[11px] font-bold"><button onClick={()=>navigator.clipboard.writeText(m.url)} className="text-cyan-deep">URL KOPYALA</button><button onClick={()=>del(m.id)} className="text-red-600">SİL</button></div></div></div>)}</div></div>}
